# Project Instructions

- 依照 `todo.md` 的 Milestone 順序實作。
- 每個 Milestone 通過驗收後，才進入下一個。
- 優先保持程式簡單、容易理解與現場 Demo。
- 除非使用者要求，不加入 Web UI、Docker、向量資料庫、LangChain 或其他框架。
- MCP Server 使用 stdio 時，不要把 debug 訊息輸出到 stdout；logging 請使用 stderr。
- API key 必須從環境變數讀取，不可寫入程式或 commit。
- Milestone 4 驗收成功後，建立指定的 Git commit。
- 修改後執行與該 Milestone 對應的驗證。